<form method="post" action="{{ path('expert_messagerie_delete', {'id': contact.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir supprimer ce message ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact.id) }}">
    <button class="btn btn-connexion mt-2"><i class="far fa-trash-alt"></i></button>
</form>